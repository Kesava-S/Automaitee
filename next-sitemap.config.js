/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://automaitee.com',
    generateRobotsTxt: true,
    outDir: 'public', // Ensures sitemap and robots.txt are output to public/
};
